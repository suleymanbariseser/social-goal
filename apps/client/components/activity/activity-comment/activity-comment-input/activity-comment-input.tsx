import { Stack } from 'tamagui';

import { Text } from '@/components/ui/text';

export const ActivityCommentInput = () => {
  return (
    <Stack pos="absolute" b={0} l={0} r={0} btw={1} btc="$borderColor">
      <Text>ActivityCommentInput</Text>
    </Stack>
  );
};
